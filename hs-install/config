## srvctl config 
## Use as string with "" if value contains spaces.

## use the latst version, options are 'latest-package' 'package' 'git' 'tar' 'src' 
LXC_INSTALL='latest-package'
## eventually specify the version - mandatory for tar, optional for dnf
LXC_VERSION='2.0.0'

## logpath
LOG=/var/log/srvctl

## temporal backup and work directory for removing containers
TMP=/temp

## Company codename - use your own
#CMP=Unknown

## Company domain name - use your own
#CDN=Unknown

## Server domain name - Access servers under a domain with his own certificates.
#SDN=Unknown

## numeric Server ID between 10 and 250 used in IP addresses - as X: 10.X.a.b
HOSTNET=10

## IPv6 address of the host
# HOSTIPv6=::1

## IPv6 address range base
# RANGEv6=::1
# PREFIXv6=64

## path to store compressed data archives
BACKUP_PATH=/temp

## turn off debug messages
debug=false

## srvctl CA related
rootca_host=$CDN
rootca_subj="/C=HU/ST=Hungary/L=Budapest/O=$CMP CA"

